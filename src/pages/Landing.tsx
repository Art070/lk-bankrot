import { ArrowRight, CheckCircle2, FileCheck2, LockKeyhole, MessageCircle, PhoneCall, ShieldCheck, Sparkles } from 'lucide-react'
import { useState, type FormEvent } from 'react'
import { Link } from 'react-router-dom'
import { Logo } from '../components/Common/Logo'

export function Landing() {
  const [sending, setSending] = useState(false)
  const [error, setError] = useState('')
  const [sent, setSent] = useState(false)
  const submit = async (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault(); const form = new FormData(event.currentTarget); setSending(true); setError('')
    try { const response = await fetch('/.netlify/functions/create-lead', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ name: form.get('name'), phone: form.get('phone'), consent: form.get('consent') === 'on' }) }); const body = await response.json() as { error?: string }; if (!response.ok) throw new Error(body.error ?? 'Не удалось отправить заявку'); setSent(true); event.currentTarget.reset() } catch (cause) { setError(cause instanceof Error ? cause.message : 'Не удалось отправить заявку') } finally { setSending(false) }
  }
  return <div className="min-h-screen bg-[#f7f9fc] text-navy-800"><header className="mx-auto flex max-w-6xl items-center justify-between px-5 py-5 sm:px-8"><Logo /><Link to="/login" className="btn-ghost px-4 py-2">Войти</Link></header><main><section className="mx-auto grid max-w-6xl gap-10 px-5 pb-14 pt-8 sm:px-8 lg:grid-cols-[1.1fr_.9fr] lg:items-center lg:pb-24 lg:pt-16"><div><p className="inline-flex items-center gap-2 rounded-full bg-gold-100 px-3 py-1.5 text-sm font-semibold text-gold-700"><Sparkles className="h-4 w-4" /> Прозрачное сопровождение процедуры</p><h1 className="mt-5 text-4xl font-extrabold leading-[1.08] text-navy-900 sm:text-5xl">Когда долгов много, <span className="text-gold-600">ясность</span> особенно важна.</h1><p className="mt-5 max-w-xl text-base leading-7 text-navy-600 sm:text-lg">«Заshitим» помогает пройти процедуру банкротства с понятной картиной происходящего: что делают юристы, какие документы нужны, когда следующий шаг и как идёт оплата по договору.</p><div className="mt-7 grid gap-3 text-sm text-navy-700 sm:grid-cols-2"><Trust text="Персональный маршрут по делу" /><Trust text="Документы и события в одном месте" /><Trust text="Связь с юристом без лишних звонков" /><Trust text="Защищённый доступ к информации" /></div></div><section id="contact" className="rounded-3xl bg-gradient-to-br from-navy-800 to-navy-950 p-5 text-white shadow-2xl sm:p-7"><PhoneCall className="h-8 w-8 text-gold-300" /><h2 className="mt-4 text-2xl font-bold">Начните с короткого разговора</h2><p className="mt-2 text-sm leading-6 text-white/70">Оставьте номер. Специалист уточнит ситуацию и объяснит, какой следующий шаг подходит именно вам.</p>{sent ? <div className="mt-6 rounded-2xl bg-emerald-400/15 p-5"><CheckCircle2 className="h-7 w-7 text-emerald-300" /><h3 className="mt-3 font-bold">Заявка отправлена</h3><p className="mt-1 text-sm text-white/75">Мы получили номер и свяжемся с вами для первичного разговора.</p></div> : <form onSubmit={submit} className="mt-6 space-y-3"><input name="name" placeholder="Как к вам обращаться (необязательно)" className="input-field bg-white text-navy-800" /><input name="phone" required type="tel" placeholder="Номер телефона" className="input-field bg-white text-navy-800" />{error && <p className="rounded-xl bg-rose-400/15 p-3 text-sm text-rose-100">{error}</p>}<label className="flex gap-2 text-xs leading-5 text-white/65"><input required name="consent" type="checkbox" className="mt-0.5" />Согласен(на) на обработку номера телефона для обратной связи.</label><button disabled={sending} className="btn-gold w-full">{sending ? 'Отправляем…' : <>Получить консультацию <ArrowRight className="h-4 w-4" /></>}</button></form>}<Link to="/demo" className="mt-5 inline-flex items-center gap-2 text-sm font-medium text-gold-200 hover:text-gold-100"><FileCheck2 className="h-4 w-4" />Посмотреть демо-кабинет</Link></section></section><section className="border-y border-navy-100 bg-white py-14"><div className="mx-auto max-w-6xl px-5 sm:px-8"><p className="text-center text-sm font-bold uppercase tracking-[.18em] text-gold-600">Как это работает</p><h2 className="mx-auto mt-3 max-w-2xl text-center text-3xl font-bold">От первого обращения до личного кабинета — без путаницы</h2><div className="mt-9 grid gap-5 md:grid-cols-4"><Step number="1" title="Оставляете номер" text="Коротко рассказываете о ситуации в удобном формате." /><Step number="2" title="Общаетесь с юристом" text="Разбираем документы и возможный порядок действий." /><Step number="3" title="Заключаете договор" text="Фиксируем условия сопровождения и оплаты." /><Step number="4" title="Получаете доступ" text="В кабинете видны этап, документы, события и договор." /></div></div></section><section className="mx-auto max-w-6xl px-5 py-14 sm:px-8"><div className="grid gap-6 rounded-3xl bg-navy-50 p-6 sm:p-9 lg:grid-cols-3"><Feature icon={ShieldCheck} title="Без лишней тревоги" text="Не нужно помнить всё в голове: задачи и события собираются в одном кабинете." /><Feature icon={MessageCircle} title="Понятная связь" text="Вопрос можно задать юристу в чате, а не искать переписку в мессенджерах." /><Feature icon={LockKeyhole} title="Доступ только для вас" text="Кабинет создаётся после заключения договора и защищён паролем." /></div></section></main><footer className="border-t border-navy-100 px-5 py-7 text-center text-xs text-navy-400">© 2026 Заshitим · Юридическое сопровождение и личный кабинет клиента</footer></div>
}
function Trust({ text }: { text: string }) { return <p className="flex items-center gap-2"><CheckCircle2 className="h-4 w-4 shrink-0 text-emerald-600" />{text}</p> }
function Step({ number, title, text }: { number: string; title: string; text: string }) { return <div className="rounded-2xl border border-navy-100 p-5"><span className="grid h-8 w-8 place-items-center rounded-full bg-gold-100 text-sm font-bold text-gold-700">{number}</span><h3 className="mt-4 font-bold">{title}</h3><p className="mt-2 text-sm leading-6 text-navy-500">{text}</p></div> }
function Feature({ icon: Icon, title, text }: { icon: typeof ShieldCheck; title: string; text: string }) { return <div><Icon className="h-6 w-6 text-gold-600" /><h3 className="mt-3 font-bold">{title}</h3><p className="mt-1 text-sm leading-6 text-navy-500">{text}</p></div> }
